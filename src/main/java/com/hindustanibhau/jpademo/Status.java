package com.hindustanibhau.jpademo;

public enum Status {
    ACTIVATED,
    DEACTIVATED,
    EXPIRED

    //this is a enum file.
    //enum is custom data type
    //when our inputs  are certain/defined. so we want no one can enter values other than this defined values. so we define enum
}
